To start the server:

cd MasterServer
node main.js

Then, :

cd ../
cd Server/build
gdb run ./rrolf-server

After that, use "run" if needed.

Finally, for the client:

cd ../.
cd Client/build
python3 -m http.server


if it doesn't work then too bad
